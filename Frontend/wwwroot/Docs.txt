28-09-25:
In wwwroot/css/app.css, the css will look after images in the localhost:8080/css/IMAGE_NAME.
But anywhere else, like if you did a script tag in MainLayout, will just look after localhost:8080/IMAGE_NAME